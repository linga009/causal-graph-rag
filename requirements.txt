numpy>=1.24
sentence-transformers>=3.0   # auto-downloads all-MiniLM-L6-v2 on first use
# Requires Microsoft Visual C++ Redistributable on Windows:
#   https://aka.ms/vs/17/release/vc_redist.x64.exe

# --- optional: stronger causal extraction (passive voice, compound nouns) ---
# Same VC++ requirement on Windows:
# spacy>=3.7
# then: python -m spacy download en_core_web_sm

# --- optional: real LLM backends (replace MockLLM) ---
# anthropic   # set ANTHROPIC_API_KEY=sk-ant-...  (Windows: set / Unix: export)
# openai      # set OPENAI_API_KEY=sk-...
# groq        # set GROQ_API_KEY=gsk_...
