__pycache__/
*.pyc
.venv/
venv/
.env
.DS_Store
*.pdf
rinn_phd_text.txt

# Fine-tuned model weights (large binaries — never commit)
models/
*.safetensors
*.bin

# Generated training artifacts
rebel_*_train.txt

# Test / build artifacts
.pytest_cache/
*.egg-info/
dist/
build/

# Raw web-download intermediates (reproducible via scripts)
wiki_raw.json

# Raw web-download intermediates
chern_raw.json
# Generated eval output
eval_value_raw.json

# Eval corpus docs are regenerable via eval_corpus/build_corpus.py
eval_corpus/*.md
# SDD scratch / ledger
.superpowers/

# Personal (not for the repo)
CLAUDE.md
.claude/
